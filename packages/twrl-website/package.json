{
  "name": "twrl-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "watch": "tsc --noEmit --watch",
    "preview": "vite preview"
  },
  "devDependencies": {
    "shiki": "^3.4.2",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "twrl": "*"
  }
}
